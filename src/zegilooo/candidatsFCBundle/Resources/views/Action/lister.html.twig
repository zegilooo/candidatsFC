{% extends "zegilooocandidatsFCBundle::layout.html.twig" %}
{% block title %}Liste des actions{% endblock %}
{% block body %}
         <h1>Liste des actions</h1>
         <div id="liste">
	        <table>
			  <tr>
			    <th>Processus</th>
			    <th>Tâche</th>
			    <th>Libelle</th>
			    <th></th>
			    <th></th>
			  </tr>
            {% for t in Action %}
			  <tr>
			    <td>{{ t.tache.processus }} </td>	
			    <td>{{ t.tache.numDansProcessus }} - {{ t.tache.libelle }} </td>	
			    <td>{{ t.numDansTache }} - {{ t.libelle }}</td>	
			    <td><a href="{{path('zegilooocandidats_fc_modifie_action',{'id':t.id})}}">Modifier</a></td>	
			    <td><a href="{{path('zegilooocandidats_fc_supprime_action',{'id':t.id})}}" class="red suppr">Supprimer</a></td>
			  </tr>            
			{% endfor %}
			</table>
		</div>
		<a href="{{path('zegilooocandidats_fc_ajout_action')}}" class="btn btn-info btn-ajout" role="button">ajouter une action</a>
{% endblock %}